{% extends "base.html.twig" %}

{% block title %}API{% endblock %}

{% block body %}
    <h1>API - JSON endpoints</h1>
    <p>Här är en lista över alla JSON-endpoints du kan testa:</p>

    <ul>
        <li><a href="{{ basePath }}/api/lucky">/api/lucky</a> – Slumpar ett lyckotal</li>
        <li><a href="{{ basePath }}/api/quote">/api/quote</a> – Returnerar ett citat</li>
        <li><a href="{{ basePath }}/api/deck">/api/deck</a> – Returnerar hela kortleken, sorterad</li>
    </ul>

    <h2>POST-endpoints (testbara med knappar)</h2>

    <form action="{{ basePath }}/api/deck/shuffle" method="post">
        <button type="submit">Blanda kortleken (POST /api/deck/shuffle)</button>
    </form>

    <form action="{{ basePath }}/api/deck/draw" method="post" style="margin-top: 10px;">
        <button type="submit">Dra ett kort (POST /api/deck/draw)</button>
    </form>

    <form action="{{ basePath }}/api/deck/draw/5" method="post" style="margin-top: 10px;">
        <button type="submit">Dra 5 kort (POST /api/deck/draw/5)</button>
    </form>
{% endblock %}
